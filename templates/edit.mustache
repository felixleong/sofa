<!DOCTYPE html>
<html>
  <head>
    <title>{{pageTitle}} : {{blogName}}</title>
    <link rel="stylesheet" href="{{assets}}/style/screen.css" type="text/css" />
  </head>
  <body>
    {{>header}}
    <div id="tagcloud"></div>
    <div id="content">
      <form id="new-post" action="dummy-page" method="post" accept-charset="utf-8">
      <h1>{{pageTitle}}</h1>
      <p>
        <label for="title">Title</label>
        <input type="text" name="title" size="50" value="{{title}}">
      </p>
      <p>
        <label for="body">Body</label>
        <textarea name="body" rows="28" cols="80">{{body}}</textarea>
      </p>
      <p>
        <label for="tags">Click tags to add them. (split by ',')</label>
        <input type="text" name="tags" value="{{tags}}" size="50" />
      </p>
      <p>
        <input id="preview" type="button" name="preview" value="Preview">
        <input type="submit" value="Save &rarr;">
        <span id="saved" style="display: none;">Saved</span>
      </p>
      </form>
      <a href="http://warpedvisions.org/projects/markdown-cheat-sheet/" target="_new">Markdown help</a>
      <div id="show-preview"></div>
    </div>
    {{>footer}}
    <script type="text/javascript" charset="utf-8">
    var postDoc = {{{doc}}}; // Bleh, global variable - figure how to get round this later
    </script>
    <script src="{{assets}}/scripts/jquery.scrollTo.js" type="text/javascript" charset="utf-8"></script>
    <script src="{{assets}}/scripts/edit.js" type="text/javascript" charset="utf-8"></script>
  </body>
</html>
